[build-system]
requires = [
    "setuptools>=42",    
]
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scikit-learn",
    "jupyter",
    "scipy",
    "joblib",
]
build-backend = "setuptools.build_meta"